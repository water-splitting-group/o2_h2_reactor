# General

This directory contains the files and instructions needed to build the irradiation setup

`chamber` contains the files and instructions needed to build the irradiaiton chamber

`irradiance_determination_chamber` contains the files and instructions to build the irradiance determination chamber

`reactor` contains the files and instructions to build the reactor

`setup` contains the instructions to build the irradiaiton setup

`synthesis` contains the labjournal entries of the synthesis of the ligands and complexes
