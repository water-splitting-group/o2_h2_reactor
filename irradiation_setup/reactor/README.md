# General

This directory contains the files and instructions needed to build the reactor

`description_reactor` is a txt file detailing the description of the reactor

`needed_parts` is a list of the needed parts to build the reactor, without sensors

`sketch_reactor` is a photo of the reactor with measurments written next to it
