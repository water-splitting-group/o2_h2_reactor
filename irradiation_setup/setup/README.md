# General

This directory contains the files and instructions needed to build the reactor

`assembly` is a txt file detailing the assembly of the setup without the chamber and reactor

`needed_parts` is a list of the needed parts to build the setup without the chamber and reactor

`pciture_setup` is a photo of the fully equipped setup
