# General

This directory contains the files of the apertures needed to build the irradiation chamber

`aperture_double_walled_beaker_v1-0` is the aperture for the double walled beaker

`aperture_dummy_v1-0` is the aperture without hole used as dummy when no other aperture is installed

`aperture_LED_v1-0` is the aperture for the LED

