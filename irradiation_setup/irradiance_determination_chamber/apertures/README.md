# General

This directory contains the files of the apertures needed to build the irradiance determination chamber

`aperture_dummy_v1-0` is the aperture without hole used as dummy when no other aperture is installed

`aperture_LED_v1-0` is the aperture for the LED

`aperture_power_sensor_v1-1` is the aperture for the power sensor
